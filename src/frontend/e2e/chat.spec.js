// Playwright-style e2e skeleton for manual enablement in CI.
// To run: install @playwright/test and execute npx playwright test

describe('CourseFlow chat e2e (skeleton)', () => {
  it('covers end-to-end chat flow in browser automation', () => {
    expect(true).toBe(true);
  });
});
